<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.somenews">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_may"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_may_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.AddNewsActivity"></activity>
        <activity android:name=".view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.FeedActivity"></activity>
        <activity android:name=".view.FeedAdminActivity"></activity>
    </application>

</manifest>